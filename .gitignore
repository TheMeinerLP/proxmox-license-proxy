# build output
/bin/
/dist/
/proxmox-license-proxy
# shell completions, generated by the GoReleaser `before` hook
/completions/

# generated / runtime artifacts (only the repo-root ones; packaging/ ships its own)
*.pem
/config.yaml
/registry.json
/registry.json.lock
*.tmp
cover.out

# editor / IDE
.idea/
.vscode/
